// @flow

import invariant from 'invariant';

function asNumber(value: any): number {
  invariant(typeof value === 'number' && !isNaN(value), 'not a number');
  return value;
}

export default asNumber;
