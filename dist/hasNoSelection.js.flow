// @flow

import hasSelection from './hasSelection';
import {EditorState} from 'draft-js';

export default function hasNoSelection(editorState: EditorState): boolean {
  return !hasSelection(editorState);
}
