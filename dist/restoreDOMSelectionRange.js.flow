// @flow

function restoreDOMSelectionRange(range: Range): void {
  const selection = window.getSelection();
  selection.removeAllRanges();
  selection.addRange(range);
}

export default restoreDOMSelectionRange;
